%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%2345678901234567890123456789012345678901234567890123456789012345678901234567890
%        1         2         3         4         5         6         7         8

\documentclass[letterpaper, 10 pt, conference]{ieeeconf}  % Comment this line out
                                                          % if you need a4paper
%\documentclass[a4paper, 10pt, conference]{ieeeconf}      % Use this line for a4
                                                          % paper

\IEEEoverridecommandlockouts                              % This command is only
                                                          % needed if you want to
                                                          % use the \thanks command
\overrideIEEEmargins
% See the \addtolength command later in the file to balance the column lengths
% on the last page of the document



% The following packages can be found on http:\\www.ctan.org
%\usepackage{graphics} % for pdf, bitmapped graphics files
%\usepackage{epsfig} % for postscript graphics files
%\usepackage{mathptmx} % assumes new font selection scheme installed
%\usepackage{times} % assumes new font selection scheme installed
%\usepackage{amsmath} % assumes amsmath package installed
%\usepackage{amssymb}  % assumes amsmath package installed
\usepackage{graphicx}
\graphicspath{{./}{../plots/utility/}}
\usepackage{float}
\usepackage{url}
\usepackage{hyperref}
\setlength{\textfloatsep}{8pt plus 2pt minus 2pt}
\setlength{\floatsep}{8pt plus 2pt minus 2pt}
\setlength{\intextsep}{8pt plus 2pt minus 2pt}
\setlength{\abovecaptionskip}{4pt plus 1pt minus 1pt}
\setlength{\belowcaptionskip}{0pt}

\title{\LARGE \bf
An Analysis of Colonel Blotto Strategies in Non-Equilibrium Scenarios
}

%\author{ \parbox{3 in}{\centering Huibert Kwakernaak*
%         \thanks{*Use the $\backslash$thanks command to put information here}\\
%         Faculty of Electrical Engineering, Mathematics and Computer Science\\
%         University of Twente\\
%         7500 AE Enschede, The Netherlands\\
%         {\tt\small h.kwakernaak@autsubmit.com}}
%         \hspace*{ 0.5 in}
%         \parbox{3 in}{ \centering Pradeep Misra**
%         \thanks{**The footnote marks may be inserted manually}\\
%        Department of Electrical Engineering \\
%         Wright State University\\
%         Dayton, OH 45435, USA\\
%         {\tt\small pmisra@cs.wright.edu}}
%}

\author{Ethan Charles, Bryce Martin, and Tristan Mott% <-this % stops a space
}

\newcommand{\utilityheatmap}[4]{%
\begin{figure}[H]
\centering
\includegraphics[width=0.82\columnwidth]{#1}
\caption{Average defender utility per step in the #2 game at #3.}
\label{fig:utility:#4}
\vspace{-6pt}
\end{figure}
}


\begin{document}



\maketitle
\thispagestyle{empty}
\pagestyle{empty}


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\begin{abstract}

We are interested in researching a game theoretical scenario for which no obvious equilibrium state exists. Specifically, we decided to look at a series of Colonel Blotto games, both the canonical game as well as a game with a retaining structure. We wanted to see what controllers could primarily maximize utility, and secondarily minimize the game-to-game variance.

\end{abstract}


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{INTRODUCTION}

Consider a classic variation of colonel blotto game with two battlefields and two generals, one defending general and one attacking general. Both generals have n troops and must decide how to allocate these troops between the two battlefields. For each battlefield, the defender wins if they have at least as many troops as the attacker (meaning they win in a tie) and the attacker wins if they have more troops than the defender. The total utility of the game is 1 if the defender defends both battlefields, 0 if they defend one of the battlefields, and -1 if they defend neither battlefield. It is worth noting that a utility of -1 isn’t possible in the classic game, as when attacker and defender have the same troop count, it’s impossible for the attack to win both fields. The defending general wants to maximize utility while the attacking general wants to minimize it, and they must simultaneously commit to a strategy without knowing the strategy of the other general.

<<<<<<< HEAD
To illustrate the game mechanics, consider a simple one-shot game with 2 troops and 2 battlefields. Table~\ref{table_utility} shows the utility outcomes for all possible strategy combinations, and Table~\ref{table_capture} shows the corresponding capture rates for attacking troops.

\begin{table}[htbp]
\caption{Utility Matrix for One-Shot Colonel Blotto Game (2 troops, 2 battlefields). Rows represent defender allocations, columns represent attacker allocations. Values indicate utility from the defender's perspective.}
\label{table_utility}
\begin{center}
\begin{tabular}{|c||c|c|c|}
\hline
\textbf{Def $\backslash$ Att} & \textbf{(2,0)} & \textbf{(1,1)} & \textbf{(0,2)} \\
\hline
\hline
\textbf{(2,0)} & 1 & 0 & 0 \\
\hline
\textbf{(1,1)} & 0 & 1 & 0 \\
\hline
\textbf{(0,2)} & 0 & 0 & 1 \\
\hline
\end{tabular}
\end{center}
\end{table}

\begin{table}[htbp]
\caption{Capture Rate Matrix for One-Shot Colonel Blotto Game (2 troops, 2 battlefields). Rows represent defender allocations, columns represent attacker allocations. Values indicate the fraction of attacking troops captured.}
\label{table_capture}
\begin{center}
\begin{tabular}{|c||c|c|c|}
\hline
\textbf{Def $\backslash$ Att} & \textbf{(2,0)} & \textbf{(1,1)} & \textbf{(0,2)} \\
\hline
\hline
\textbf{(2,0)} & 1.0 & 0.5 & 0.0 \\
\hline
\textbf{(1,1)} & 0.5 & 1.0 & 0.5 \\
\hline
\textbf{(0,2)} & 0.0 & 0.5 & 1.0 \\
\hline
\end{tabular}
\end{center}
\end{table}

The defending troops will do whatever they are told. The attacking troops, however, receive utility m for obeying orders (as opposed to defecting) and are penalized utility p if they are captured during an attack. If X is the perceived mixed strategy of the attacking general and Y is the perceived mixed strategy of the defending general, the attacking troops will defect if the expected capture probability C(X, Y) <= $\vartheta$, where $\vartheta$ is equal to m/p. The defending general may manipulate their mixture Y in order to take advantage of this deterrence dynamic. The attacking general must be careful about their own mixed strategy in order to prevent their troops from defecting.

When $\frac{1}{2} \leq \vartheta < \frac{3\lfloor n/2 \rfloor+1}{4\lfloor n/2 \rfloor+2}$ (which goes to $\frac{3}{4}$ in the limit), there do not exist Nash equilibrium values of X and Y. In other words, it is not clear what rational generals would do in this situation.

\subsection{Troop Retention Variation}

In a variation on the classic colonel blotto game, both generals retain any troops they capture from the opponent. This variation adds a different dynamic to the game that is far more likely to result in an equilibrium state (ie either the attacker or defender have enough troops to win both battlefields every time). This leads to strategies that are less concerned with short term utility more about troops acquisition.

\subsubsection{Subsubsection here} Subsubsection text

=======
>>>>>>> a770457d18ce7521981f1cd060cb37bdb8dc8753
\section{RELATED WORK}
Related work text here

\section{METHODOLOGY}
Methodology text here
\subsection{Simulation Parameters}
Subsection text here about the different parameters in the simulation
\subsection{Simulation Types}
Subsection text here about the different types of the simulation (retaining and not retaining?)
\subsection{Controllers}
Subsection text here about the different controllers we made
\subsubsection{Random}
The Random agent selects uniformly at random from all valid troop allocations available to it. The agent does not attempt to model the opponent or reason about expected outcomes. Instead, it acts as a baseline policy for comparison with more sophisticated agents. Because its behavior is entirely stochastic, its performance provides a lower-bound benchmark for evaluating the effectiveness of the other agents.
\subsubsection{Even}
The Even agent deterministically divides its troops evenly across the two battlefields. If the total number of troops is not divisible by two, remaining troops are sent to the right battlefield. This agent represents a simple heuristic strategy that attempts to maintain balanced coverage of all battlefields without adapting to the opponent's behavior. It serves as a deterministic baseline against which more adaptive strategies can be compared.
\subsubsection{Monte Carlo}
The Monte Carlo agent is designed for the non-retaining variant of the simulation. In this setting, the perceived capture probability is ignored to simplify the evaluation process. This simplification reduces the complexity of the state space and allows the agent to evaluate all possible action combinations for the next turn.

Rather than simulating entire games, the agent performs a one-step lookahead search. For each possible move available to the agent, the algorithm enumerates all possible combinations of moves by both players in the next turn. The resulting utility of each combination is computed, and the utilities corresponding to each candidate move are aggregated. The agent then selects the move that maximizes the total utility across all combinations that include that move. If multiple moves produce the same utility, one is selected uniformly at random.

Although this agent is referred to as a Monte Carlo agent, it does not perform stochastic sampling of actions. Traditional Monte Carlo methods estimate expected outcomes through repeated random sampling of possible scenarios. In contrast, this agent deterministically enumerates all possible one-step action combinations and therefore does not constitute a true Monte Carlo simulation. Instead, it serves as a simple baseline evaluation method for the non-retaining variant of the game.
\subsubsection{Monte Carlo Tree Search}
This agent can be viewed as an extension of the Monte Carlo agent designed to handle the additional complexity introduced by troop retention. Because this additional complexity makes flat Monte Carlo evaluation impractical, we instead employ a Monte Carlo Tree Search (MCTS) algorithm to explore possible actions and estimate their expected utility.

The search tree represents sequences of moves for the agent acting as either the attacker or defender. At each node corresponding to our agent's decision, possible troop allocations are expanded as child nodes. The opposing player's action is modeled using a stochastic policy, where the opponent's move is selected randomly. Future work could improve this component by incorporating a more accurate model of opponent behavior.

As shown in figure~\ref{fig:mcts_tree}, Monte Carlo Tree Search consists of four phases: selection, expansion, simulation, and backpropagation.

\paragraph{Selection and Expansion}
The search proceeds for a fixed computational budget defined by a maximum number of node expansions (typically around 100 in our experiments). During each iteration, the algorithm traverses the existing tree from the root using a modified Upper Confidence Bound (UCB) selection rule. The UCB value for each node is computed from the average observed utility and the number of times the node has been visited relative to its parent, balancing exploration of under-sampled nodes with exploitation of high-performing ones.

\paragraph{Simulation and Backpropagation}
Once a leaf node is selected, the algorithm performs several random rollout simulations from that state to estimate the expected utility of the position. The resulting utility is then backpropagated through the visited nodes, updating their statistics.

After the search budget is exhausted, the agent selects the action corresponding to the child of the root node with the highest visit count, which represents the branch with the strongest empirical performance.
% TODO fix the width on this image
\begin{figure}[t]
\centering
\includegraphics[width=0.5\textwidth]{mcts_tree}
\caption{Example structure of Monte Carlo Tree Search. Adapted from \cite{mcts_wiki}.}
\label{fig:mcts_tree}
\end{figure}
\subsubsection{Dynamic Programming Nash (DP Nash)}
The Dynamic Programming Nash agent solves the finite-horizon extensive-form game by backward induction over the remaining stages. Rather than selecting an action from a one-step rollout or a sampled search tree, it computes continuation values for reachable states and uses those continuation values to determine an optimal mixed policy at the current stage.

In the non-retaining game, the dynamic-programming state is defined by the current stage and the current perceived capture probability. In the retaining game, the state additionally includes the current number of attacking troops, with the number of defending troops determined implicitly by the total troop count. For each state, every joint troop allocation by the attacker and defender produces an immediate defender utility together with a transition to a next state determined by the updated perceived capture probability and, in the retaining case, the post-capture troop counts.

At each state, the agent forms the defender-payoff matrix over all legal attacker and defender allocations and solves the induced zero-sum matrix game using linear programming to obtain mixed strategies for both sides. The resulting state value and equilibrium policies are then stored and used as continuation values for earlier stages in the backward-induction recursion. The key approximation in this method is that the perceived capture probability is not kept continuous; instead, the interval $[0,1]$ is discretized into 100 bins, and each updated perceived capture probability is mapped to the nearest bin before the continuation value is queried.
\subsubsection{Dynamic Programming Exploit (DP Exploit)}
The Dynamic Programming Exploit agent uses the same dynamic-programming state representation, transition model, and backward-induction structure as DP Nash. The difference is that it does not solve the mixed equilibrium at each state. Instead, it assumes the opposing player chooses uniformly at random from all legal troop allocations and computes a best response to that random policy at each state. When multiple pure actions achieve the same best-response value, the agent mixes uniformly across those tied actions.
\subsubsection{Retaining Heuristic}
The Heuristic agent prioritizes securing a battlefield outright before distributing troops more evenly. Concretely, when the agent does not yet have enough remaining troops to guarantee victory on both battlefields, it allocates all available troops to one battlefield chosen uniformly at random. Once its remaining troop count is sufficiently large that it could allocate equal numbers of troops to both battlefields and still exceed the opponent’s previous allocations, the agent switches to an even split across both battlefields.

\section{RESULTS}
The defender utility heatmaps in this section summarize average utility per step across the full controller matchup matrix. For each parameterization and each attacker-defender controller pairing, we simulated 1000 games, with each game running for 100 stages. We organize the results by game variant first and then by the parameter being swept so that the effect of each modeling choice can be compared under both independent-round and retaining dynamics.

\subsection{Non-Retaining Game}
In the non-retaining variant, each round begins with a fresh allocation problem and no troops persist from one step to the next. As a result, the reported averages isolate the immediate effect of each controller matchup without any accumulation of material advantage across time.

\subsubsection{Attacker Troop Count ($N_a$)}
We first sweep the attacker troop count $N_a$ over $\{6, 8, 10, 12, 14\}$ while holding $N_d = 10$, $m = 0.5$, $p = 1.0$, $\alpha = 0.5$, and $c_0 = 0.25$ fixed. This sweep measures how the defender utility matrix changes as the attacker is given fewer or more troops than the defender.

\utilityheatmap{utility_nonretain_n_att_6}{non-retaining}{$N_a = 6$}{nonretain:n_att:6}
\utilityheatmap{utility_nonretain_n_att_8}{non-retaining}{$N_a = 8$}{nonretain:n_att:8}
\utilityheatmap{utility_nonretain_n_att_10}{non-retaining}{$N_a = 10$}{nonretain:n_att:10}
\utilityheatmap{utility_nonretain_n_att_12}{non-retaining}{$N_a = 12$}{nonretain:n_att:12}
\utilityheatmap{utility_nonretain_n_att_14}{non-retaining}{$N_a = 14$}{nonretain:n_att:14}

\subsubsection{Defender Troop Count ($N_d$)}
We next sweep the defender troop count $N_d$ over $\{6, 8, 10, 12, 14\}$ while holding $N_a = 10$, $m = 0.5$, $p = 1.0$, $\alpha = 0.5$, and $c_0 = 0.25$ fixed. This sweep is the complementary resource experiment and shows how strongly the defender utility responds to additional defensive capacity.

\utilityheatmap{utility_nonretain_n_def_6}{non-retaining}{$N_d = 6$}{nonretain:n_def:6}
\utilityheatmap{utility_nonretain_n_def_8}{non-retaining}{$N_d = 8$}{nonretain:n_def:8}
\utilityheatmap{utility_nonretain_n_def_10}{non-retaining}{$N_d = 10$}{nonretain:n_def:10}
\utilityheatmap{utility_nonretain_n_def_12}{non-retaining}{$N_d = 12$}{nonretain:n_def:12}
\utilityheatmap{utility_nonretain_n_def_14}{non-retaining}{$N_d = 14$}{nonretain:n_def:14}

\subsubsection{Obedience Reward ($m$)}
We then sweep the attacker obedience reward $m$ over $\{0.25, 0.5, 0.75, 1.0\}$ while holding $N_a = 10$, $N_d = 10$, $p = 1.0$, $\alpha = 0.5$, and $c_0 = 0.25$ fixed. Since $m$ controls how attractive it is for attacking troops to follow orders rather than defect, it directly affects how much deterrence the defender can exploit.

\utilityheatmap{utility_nonretain_m_0p25}{non-retaining}{$m = 0.25$}{nonretain:m:0p25}
\utilityheatmap{utility_nonretain_m_0p5}{non-retaining}{$m = 0.5$}{nonretain:m:0p5}
\utilityheatmap{utility_nonretain_m_0p75}{non-retaining}{$m = 0.75$}{nonretain:m:0p75}
\utilityheatmap{utility_nonretain_m_1p0}{non-retaining}{$m = 1.0$}{nonretain:m:1p0}

\subsubsection{Initial Perceived Capture Probability ($c_0$)}
We also sweep the initial perceived capture probability $c_0$ over $\{0.0, 0.25, 0.5\}$ while holding $N_a = 10$, $N_d = 10$, $m = 0.5$, $p = 1.0$, and $\alpha = 0.5$ fixed. In the model, $c_0$ is the initial value of the capture probability perceived by the attacking troops before the iterative update begins.

\utilityheatmap{utility_nonretain_c0_0p0}{non-retaining}{$c_0 = 0.0$}{nonretain:c0:0p0}
\utilityheatmap{utility_nonretain_c0_0p25}{non-retaining}{$c_0 = 0.25$}{nonretain:c0:0p25}
\utilityheatmap{utility_nonretain_c0_0p5}{non-retaining}{$c_0 = 0.5$}{nonretain:c0:0p5}

\subsubsection{Memory Parameter ($\alpha$)}
Finally, we sweep the memory parameter $\alpha$ over $\{0.1, 0.3, 0.5, 0.7, 0.9\}$ while holding $N_a = 10$, $N_d = 10$, $m = 0.5$, $p = 1.0$, and $c_0 = 0.25$ fixed. In the model, $\alpha$ determines how strongly the attacking troops weight prior perceived capture probability when updating their current belief.

\utilityheatmap{utility_nonretain_alpha_0p1}{non-retaining}{$\alpha = 0.1$}{nonretain:alpha:0p1}
\utilityheatmap{utility_nonretain_alpha_0p3}{non-retaining}{$\alpha = 0.3$}{nonretain:alpha:0p3}
\utilityheatmap{utility_nonretain_alpha_0p5}{non-retaining}{$\alpha = 0.5$}{nonretain:alpha:0p5}
\utilityheatmap{utility_nonretain_alpha_0p7}{non-retaining}{$\alpha = 0.7$}{nonretain:alpha:0p7}
\utilityheatmap{utility_nonretain_alpha_0p9}{non-retaining}{$\alpha = 0.9$}{nonretain:alpha:0p9}

\subsection{Retaining Game}
In the retaining variant, surviving troops remain available in later rounds, so early outcomes can compound into persistent material advantages. The reported heatmaps therefore reflect both immediate controller choices and the longer-horizon path dependence introduced by retention.

\subsubsection{Attacker Troop Count ($N_a$)}
We again sweep the attacker troop count $N_a$ over $\{6, 8, 10, 12, 14\}$ while holding $N_d = 10$, $m = 0.5$, $p = 1.0$, $\alpha = 0.5$, and $c_0 = 0.25$ fixed. In the retaining setting, this sweep reveals how a persistent resource imbalance amplifies or suppresses specific controller matchups over time.

\utilityheatmap{utility_retain_n_att_6}{retaining}{$N_a = 6$}{retain:n_att:6}
\utilityheatmap{utility_retain_n_att_8}{retaining}{$N_a = 8$}{retain:n_att:8}
\utilityheatmap{utility_retain_n_att_10}{retaining}{$N_a = 10$}{retain:n_att:10}
\utilityheatmap{utility_retain_n_att_12}{retaining}{$N_a = 12$}{retain:n_att:12}
\utilityheatmap{utility_retain_n_att_14}{retaining}{$N_a = 14$}{retain:n_att:14}

\subsubsection{Defender Troop Count ($N_d$)}
We next sweep the defender troop count $N_d$ over $\{6, 8, 10, 12, 14\}$ while holding $N_a = 10$, $m = 0.5$, $p = 1.0$, $\alpha = 0.5$, and $c_0 = 0.25$ fixed. Because troops persist in this variant, additional defensive resources can snowball into long-run control of both battlefields.

\utilityheatmap{utility_retain_n_def_6}{retaining}{$N_d = 6$}{retain:n_def:6}
\utilityheatmap{utility_retain_n_def_8}{retaining}{$N_d = 8$}{retain:n_def:8}
\utilityheatmap{utility_retain_n_def_10}{retaining}{$N_d = 10$}{retain:n_def:10}
\utilityheatmap{utility_retain_n_def_12}{retaining}{$N_d = 12$}{retain:n_def:12}
\utilityheatmap{utility_retain_n_def_14}{retaining}{$N_d = 14$}{retain:n_def:14}

\subsubsection{Obedience Reward ($m$)}
We then sweep the attacker obedience reward $m$ over $\{0.25, 0.5, 0.75, 1.0\}$ while holding $N_a = 10$, $N_d = 10$, $p = 1.0$, $\alpha = 0.5$, and $c_0 = 0.25$ fixed. In the retaining game, this parameter influences not just immediate attacks but also the likelihood that a favorable or unfavorable trajectory will persist across later rounds.

\utilityheatmap{utility_retain_m_0p25}{retaining}{$m = 0.25$}{retain:m:0p25}
\utilityheatmap{utility_retain_m_0p5}{retaining}{$m = 0.5$}{retain:m:0p5}
\utilityheatmap{utility_retain_m_0p75}{retaining}{$m = 0.75$}{retain:m:0p75}
\utilityheatmap{utility_retain_m_1p0}{retaining}{$m = 1.0$}{retain:m:1p0}

\subsubsection{Initial Perceived Capture Probability ($c_0$)}
We also sweep the initial perceived capture probability $c_0$ over $\{0.0, 0.25, 0.5\}$ while holding $N_a = 10$, $N_d = 10$, $m = 0.5$, $p = 1.0$, and $\alpha = 0.5$ fixed. In the model, $c_0$ is the initial value of the capture probability perceived by the attacking troops before the iterative update begins.

\utilityheatmap{utility_retain_c0_0p0}{retaining}{$c_0 = 0.0$}{retain:c0:0p0}
\utilityheatmap{utility_retain_c0_0p25}{retaining}{$c_0 = 0.25$}{retain:c0:0p25}
\utilityheatmap{utility_retain_c0_0p5}{retaining}{$c_0 = 0.5$}{retain:c0:0p5}

\subsubsection{Memory Parameter ($\alpha$)}
Finally, we sweep the memory parameter $\alpha$ over $\{0.1, 0.3, 0.5, 0.7, 0.9\}$ while holding $N_a = 10$, $N_d = 10$, $m = 0.5$, $p = 1.0$, and $c_0 = 0.25$ fixed. In the model, $\alpha$ determines how strongly the attacking troops weight prior perceived capture probability when updating their current belief.

\utilityheatmap{utility_retain_alpha_0p1}{retaining}{$\alpha = 0.1$}{retain:alpha:0p1}
\utilityheatmap{utility_retain_alpha_0p3}{retaining}{$\alpha = 0.3$}{retain:alpha:0p3}
\utilityheatmap{utility_retain_alpha_0p5}{retaining}{$\alpha = 0.5$}{retain:alpha:0p5}
\utilityheatmap{utility_retain_alpha_0p7}{retaining}{$\alpha = 0.7$}{retain:alpha:0p7}
\utilityheatmap{utility_retain_alpha_0p9}{retaining}{$\alpha = 0.9$}{retain:alpha:0p9}

\section{OBSERVATIONS}
\subsection{Security vs. Exploitation}
One of the clearest tradeoffs in our results was the distinction between security and exploitation. The Dynamic Programming Nash controller was designed to be secure in the minimax sense, and in practice it consistently protected its floor very well across parameter settings. In the canonical non-retaining case, for example, the defender utility for \texttt{dp\_nash} stayed nearly constant across all attacking controllers, ranging only from approximately $0.236$ to $0.238$. By contrast, \texttt{dp\_exploit} achieved a higher best-case value in the same table, but its performance ranged from approximately $0.001$ to $0.360$ depending on the opponent. This is exactly the tradeoff we would expect from equilibrium play: stronger worst-case protection, but less aggressive exploitation of weak opponents.

The right intuition is the same as in Rock-Paper-Scissors. The equilibrium strategy is uniform random, and that strategy is secure because it prevents the opponent from systematically gaining an advantage. At the same time, it does not exploit a suboptimal opponent as strongly as a targeted best response would. The same phenomenon appears here. By committing to a secure mixed strategy, \texttt{dp\_nash} avoids being taken advantage of outside of sampling noise and the small approximation introduced by discretizing perceived capture probability, but that same conservatism can leave utility on the table against predictable or weak opponents.

This was especially visible in the non-retaining experiments against the \texttt{random} controller. In the canonical non-retaining setting, \texttt{dp\_exploit} achieved an average defender utility of approximately $0.310$ against a random attacker, while \texttt{dp\_nash} achieved approximately $0.238$. The same pattern held when $\alpha = 0.1$, where \texttt{dp\_exploit} reached approximately $0.373$ against \texttt{random}, compared with approximately $0.304$ for \texttt{dp\_nash}. In these cases, the exploitative controller was preferable precisely because the opponent was not playing a secure strategy of its own.

The retaining experiments showed that this tradeoff was not limited to the dynamic-programming controllers. In some retaining settings, the \texttt{retaining\_heuristic} controller was preferable to \texttt{dp\_nash} against weak opponents. When $N_d = 6$, \texttt{retaining\_heuristic} achieved approximately $0.206$ against a random attacker, while \texttt{dp\_nash} achieved approximately $-0.033$. When $N_a = 14$, \texttt{retaining\_heuristic} achieved approximately $0.448$ against \texttt{random}, compared with approximately $0.248$ for \texttt{dp\_nash}. Taken together, these results suggest that \texttt{dp\_nash} was often the safest controller when robustness mattered most, but it was not always the best controller when the opponent was known to be weak, random, or otherwise exploitable.
\subsection{Bryce Discussion Here}
\subsection{Monte Carlo Based Controllers}
An interesting behavior was observed with the Monte Carlo (MC) controller. In scenarios where the number of attackers and defenders was equal, the MC controller behaved identically to the Random controller. This occurs because, under these conditions, the Monte Carlo sampling procedure produced uniform utility estimates across the available actions. As a result, the policy induced by the MC controller was effectively equivalent to selecting actions uniformly at random.

This observation suggests that, in non-retaining games of this type, uniform random action selection can constitute a viable baseline strategy when the expected utilities of the available actions are symmetric. In other words, when no action yields a consistently higher payoff under the sampled rollouts, the MC controller does not develop a meaningful preference and defaults to behavior indistinguishable from random play.

A second observation arises in cases where the number of attackers exceeds the number of defenders. Under these conditions, the Dynamic Programming (DP) Exploit controller was able to achieve higher performance against the MC controller than against the Random controller. This result is somewhat counterintuitive, as the Exploit controller was explicitly designed to exploit uniformly random policies. One possible explanation is that the MC controller introduces subtle biases in action selection due to sampling variance or rollout structure, which the exploitative strategy can leverage more effectively than pure uniform randomness.

The Monte Carlo Tree Search (MCTS) controller exhibited highly variable performance across trials. In some cases, it achieved strong outcomes, while in others it performed significantly worse than the simpler baseline controllers. One possible explanation for this inconsistency is the simulation policy used during the expansion and rollout phases of the MCTS algorithm. If the rollout simulations do not accurately reflect the true value of game states, the resulting value estimates may misguide the tree search process. Alternatively, the observed variability may be attributable to implementation errors or insufficient exploration within the search tree. Further investigation is required to determine the primary cause of this behavior.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{CONCLUSIONS AND FUTURE WORKS}

\subsection{Conclusions}

This project studied how a range of controllers behave in Colonel Blotto variants with deterrence and troop retention, particularly in settings where equilibrium behavior is not immediately obvious. Across both retaining and non-retaining games, the most consistent theme was that controller performance depended not only on the game parameters but also on whether the objective was security or exploitation. In particular, \texttt{dp\_nash} provided strong worst-case protection, while more exploitative controllers could achieve higher utility against weak or predictable opponents.

The results also showed that the retaining and non-retaining variants produce qualitatively different strategic behavior. In the non-retaining setting, the main effect of a controller is often visible through immediate action selection, while in the retaining setting early advantages can compound over time and amplify small strategic differences. Taken together, these results suggest that there is no single universally best controller in this class of games. Instead, the best choice depends on whether the controller is being used to guarantee robustness against adversarial play or to exploit structure in the opponent's behavior.


\subsection{Future Works}
\subsubsection{Dynamic Programming Resolution}
The Dynamic Programming controllers rely on a discretization of perceived capture probability into 100 bins. This makes the state space tractable, but it also introduces approximation error when the true perceived capture probability is mapped to the nearest bin. A natural next step would be to study how sensitive the policies and values are to this resolution, and to investigate finer discretizations, adaptive discretizations, or continuous approximations that preserve tractability while reducing quantization error.

\subsubsection{Dynamic Programming Opponent Models}
The current \texttt{dp\_exploit} controller computes a best response to an opponent that is assumed to play uniformly at random over all legal troop allocations. This makes it a useful exploitation baseline, but it is a limited model of realistic opponent behavior. Future work could replace this assumption with empirical opponent models, learned policies, or iterative best-response procedures that adapt to the strategies actually observed during play.

\subsubsection{Monte Carlo Tree Search}
When selecting what move the opposite side makes for a node, we are just randomly picking. Some improvements can be made here to either pick an average move (in terms of utility) or possibly pick the worst possible move the enemy could make in response. This could be an additional parameter to change how it decides.

\subsubsection{Scaling Beyond Two Battlefields}
This study focused on the two-battlefield version of Colonel Blotto in order to make the controller comparisons tractable and interpretable. Extending these experiments to games with more battlefields would provide a stronger test of whether the same security-versus-exploitation tradeoff persists in richer strategic settings. Such extensions would also make it possible to study whether simple heuristics remain competitive as the action space grows.

\subsubsection{Longer Horizons and Statistical Validation}
Although each controller matchup was simulated many times, there is still value in pushing the experiments further through longer horizons, additional repetitions, and more explicit statistical reporting. Future work could include confidence intervals, sensitivity analysis across parameter settings, and more systematic robustness checks to better separate persistent strategic effects from sampling variability. This would be especially useful for evaluating controllers such as MCTS whose observed performance appears to be more variable.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\begin{thebibliography}{99}

\bibitem{mcts_wiki}
"Monte Carlo Tree Search", Wikipedia, Wikimedia Foundation,
% \url{https://en.wikipedia.org/wiki/Monte_Carlo_tree_search}

\end{thebibliography}

\end{document}
